name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: |
         cd hw4
         docker build -t ruishaopu561/seproject:v1 -f tinyurl.dockerfile .
         docker push ruishaopu561/seproject:v1
